import { Meta } from '@storybook/addon-docs/blocks';
import geburtsorteAbgeornete from './geburtsorte-abgeornete.png'
import scooters from './scooters.png'
import parkSpaces from './park-spaces.png'
import co2Emmisons from './co2-emmisons.png'


<Meta title="I Styleguide/3 Interactives" />

# click red button first, ask questions later

The idea is to build interactives that have one primary action.  
For example moving a range slider or clicking a play button.  
Normally this action can be encoded with something like a red button.

Maybe after the primary action, the user can replay the animation, select or highlight a
specific datapoint or filter the data.  
But maybe the primary action is all we need.

**examples:**

<img src={geburtsorteAbgeornete} width='320px' />
<img src={scooters} width='320px' />
<img src={parkSpaces} width='320px' />

<p>&nbsp;</p>

## Slider

A different primary action can be a slider.  
So a start screen (or inital data point) is already selected
and the user can slide through the datapoints.

If she is interested in a specific datapoint it can be selected
in the map or overview below or above

**examples:**

<img src={co2Emmisons} width='320px' />

